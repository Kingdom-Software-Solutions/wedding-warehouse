import styled from 'styled-components';
import { spicyMix } from './colors';

// Logo Styles
export const StyledLogo = styled.p`
    font-family: 'Cinzel Decorative', cursive;
    color: ${spicyMix}
`;