// Warehouse State
import { initialState } from './warehouseInitial'

export const warehouseReducer = (state = initialState, action) =>{
    switch (action.type) {
        default:
          return state
    }    
}