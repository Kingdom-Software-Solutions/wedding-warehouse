// Take an array, loop through it and see if that item has a reservation or not
        // for each item in the cart, check if the reservations have a conflict in the date range selected
        // if any return from the db call  
            // means it is reserved and this funtion should return false
        // else
            // true